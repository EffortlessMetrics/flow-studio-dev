Gate fixes applied:

- Ran `cargo fmt` and applied formatting fixes.
- Ran `cargo clippy` and fixed one minor lint.
# Gate Fix Summary

- Files modified by auto-linter (`cargo fmt`): none required / idempotent run.
- No doc changes applied automatically.
