Perf status:

- No significant perf concerns for a trivial health endpoint returning static JSON.
# Performance Status

- No obvious performance regressions introduced by this endpoint.
- Watch for high-volume probes; instrument with a simple counter if desired.
