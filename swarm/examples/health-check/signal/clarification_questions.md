## Clarification Questions

1. Preferred endpoint path: `/health` or `/live`? (default: `/health`)
2. Should the health endpoint perform any dependency checks (DB, cache)? (default: no)
3. Any preferred response shape beyond `{ "status": "ok" }`? (default: minimal)
