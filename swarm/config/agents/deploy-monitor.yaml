# Provider-neutral agent definition (Phase 1-full)

key: deploy-monitor

flows:
  - deploy

category: verification
color: blue
source: project/user

short_role: Watch CI and deployment events

model: haiku

outputs:
  - path: RUN_BASE/deploy/verification_report.md
    format: markdown

tags:
  - flow-5
  - deploy
  - verification
